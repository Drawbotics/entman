define('@Schema', function () {
  describe('defineSchema(name, attributes)', function () {
    it('should throw an error when `name` is empty');
    it('should throw an error when `attributes` is not an object');
    it('should return a schema object');
    it('the schema object should have a property with the name of the schema');
    it('the schema object should have a property with the attributes of the schema');
  });
  describe('hasMany()', function () {
  });
  describe('generateSchemas()', function () {
  });
});
